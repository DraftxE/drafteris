#include <iostream>
#include <iomanip>
using namespace std ;
int main()
{
    bool flag=false ;
    int x = 100 , y = 50 ;
    cout << "Flag is " << flag << " or " << boolapha << flag endl ;
    cout << "if x = " << x << " and y = " << y << endl ;
    cout << "x==y result is " << ( x==y ) << endl ;
    cout << "x! = y result is " << ( x! = y ) << endl ;
    cout << "x < y result is " << ( x<y ) << endl ;
    cout << "x<=y result is " << (x < = y) endl ;
    cout << "x>y result is " << (x > y) << endl ;
    cout << "x>=y result is " << (x > = y ) << endl ;
    system ("pause");
    return 0 ;
}
